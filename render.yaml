services:
  - type: web
    name: harz-cloud-backend
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: JWT_SECRET
        sync: false
      - key: GITHUB_TOKEN
        sync: false
